/**
 *  This the main file of the ui of the contact us section
 * To use contact us image envelope inside src attribute of img element pass the function
 * getImage("imgContactUs")
 */
import "./style.css";
import { getImage } from "../../../helpers/getImges";
const ContactUs = () => {
  return <div>Contact us</div>;
};

export default ContactUs;
