/**
 * For the Project item  card bg image you can access it by getImage(element.imageUrl)
 */
import React from "react";
import "./style.css";
import { getImage } from "../../../api/landingPage/getImges";
const ProjectItem = (props) => {
  const { element } = props;
  return <div>ProjectItem</div>;
};

export default ProjectItem;
