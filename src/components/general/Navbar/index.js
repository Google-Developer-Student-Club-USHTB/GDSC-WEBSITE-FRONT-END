/**
 *  This the main file of the ui of the Navbar section
 * To use gdsc logo image inside src attribute of img element pass the function getImage("imgLogoWithTypo")
 */
import "./style.css";
import { getImage } from "../../../helpers/getImges";
const NavigationBar = () => {
  return <div>Navigation Bar</div>;
};

export default NavigationBar;
