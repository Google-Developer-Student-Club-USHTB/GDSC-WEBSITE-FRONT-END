/**
 * Here you can code a single project item or card
 * For the Project item  card bg image you can access it by element.imageUrl
 */
import React from "react";
import "./style.css";
import { getImage } from "../../../helpers/getImges";
const ProjectItem = (props) => {
  const { element } = props;
  return <div>ProjectItem</div>;
};

export default ProjectItem;
